extends layouts/main.pug

block content
  .container
    .explain-wrapper
      .explainmain=`안녕하세요. ${user.nickname}님`
      .explainText 이메일인증을 한 후에 서비스 이용이 가능합니다.
      form#jsEmailConfirm
        input(type="text" value=user.email name="email" readonly )
        input(type="submit" value=`인증이메일 발송`)
      #jsComment.hidden
        div(style="color:red; font-size: 15px;margin-top:8px")=`이메일이 발송되었습니다.`
